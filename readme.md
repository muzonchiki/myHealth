## Project Medical APP (with out mobile adaptive)

### Доступные команды:
* `npm install` - установить зависимости
* `npm run serv` - запустить встроенный сервер и следить за изменениями файлов
* `npm run build` -  собрать проект в папку `build`